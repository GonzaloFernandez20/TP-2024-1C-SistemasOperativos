{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "options": {
    "env": {
      "log_file": "leaks-${workspaceFolderBasename}.log"
    },
    "cwd": "${workspaceFolder}"
  },
  "tasks": [
    {
      "label": "build",
      "command": "make all",
      "type": "shell",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gcc"
      ]
    },
    {
      "label": "clean",
      "command": "make clean",
      "type": "shell",
      "group": {
        "kind": "none"
      },
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "valgrind-leak-check",
      "command": "valgrind",
      "args": [
        "--leak-check=full",
        "--log-file=${log_file}",
        "./bin/${workspaceFolderBasename}",
        "./${workspaceFolderBasename}.config"
      ],
      "type": "shell",
      "hide": true,
      "dependsOn": [
        "build"
      ]
    },
    {
      "label": "leak check",
      "command": "code",
      "type": "shell",
      "args": [
        "${log_file}"
      ],
      "group": "test",
      "dependsOn": [
        "valgrind-leak-check"
      ],
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
